(function () {
    console.log("A cozy nest is ready");
  })();
  
  /*Immediately Invoked Function Expression (IIFE): the syntax means that the func is created and executed right away simultaneously; this is an anonymous function expression;
  the function has no name and is not stored in a variable. 
  In the modern code it's used rarely.
  */